const MediaHandler = require('./MediaHandler');

class MessageHandler extends MediaHandler {
    // Esta clase extiende MediaHandler para mantener compatibilidad
    // Puede ser extendida en el futuro con funcionalidades adicionales
}

module.exports = MessageHandler;